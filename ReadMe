Missign doxygen:

  satellite_factory set_counter(int)
  
  satellite void workLoad(bool);
  satellite void setName(string s);

  mediators void SetMed(mediatorMemento *m);
  mediators mediatorMemento *getMed();

  Itertator vector<satellite *>getSats();
  
  mediatorMemento vector<bool> getOrbits();
  mediatorMemento vector<bool> getOStatus();

  The whole ass falcon_command class
  The whole ass falcon_factory class
